# Checkpoint Artifact Status

The existing DOCX and PDF checkpoint files record the original filename-random split and are retained only as historical project-development artifacts. A later audit found source-chart overlap and exact-pair duplicates across those partitions.

Do not cite their quantitative values as final evidence. Regenerate the result-bearing checkpoints only after the `grouped_v1_contiguous_24` three-model, three-seed experiment matrix has completed and `metric_summary_mean_std.csv` is available.
